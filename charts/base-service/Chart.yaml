apiVersion: v1
name: generic-microservice
description: A generic template for trocks springboot microservices
version: 1.0.0 # change this when something is changed in the underlying templates (e.g. configmap.yaml)
# Standard application type
type: application
appVersion: "1.0.0" #code version or our app release version
keywords:
  - springboot
  - spring-cloud
  - gateway
  - config